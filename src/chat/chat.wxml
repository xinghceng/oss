<view>
<view>
<view wx:for="{{records}}">
<view class="{{item.user}}">
<text >{{item.user}}</text>
<image src="{{user.img}}" class="one"></image>
<text>{{item.msg}}</text>
</view>
</view>
</view>
<form bindsubmit="sendMyMsg" >
<input type="text" value="{{value}}" name="text"/>
<button size="mini" type="default" form-type="submit">发送</button>
</form>
</view>
